name: Build and Deploy to Azure Env
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  push:
    branches: ['master']
  workflow_dispatch:

permissions:
  contents: read

jobs:
  buildImage:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Azure Container Registry Login
        uses: Azure/docker-login@v1
        with:
          # Container registry username
          username: 'ccconreg'
          # Container registry password
          password: 'dRCHShz45snIg3FA+QI64Nm1zZseE6myR66Gmhm/lC+ACRCqHv13'
          # Container registry server url
          login-server: 'ccconreg.azurecr.io'
      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          push: true
          tags: ccconreg.azurecr.io/hello-world-app:${{ github.sha }}
          file: ./Dockerfile
          
  deployToAKS:
    permissions:
      actions: read
      contents: read
      id-token: write
      
    runs-on: ubuntu-latest
    needs: [buildImage]
    environment: Production
    steps:
      - name: Check out repo code
        uses: actions/checkout@v3
        
      - name: Azure login
        uses: azure/login@v1.4.6
        with:
          client-id: 'caf1b1e7-3e49-453e-967f-d442db52e447'
          tenant-id: 'd3275226-ed38-4275-aa8c-883c16f01d9e'
          subscription-id: '98a3196f-6832-4fbc-b17d-84d0c75b73ab'
      - run: echo "Azure login successfully"
      
      - name: Setup kubelogin
        uses: Azure/use-kubelogin@v1
        with:
          kubelogin-version: 'v0.0.25'

      - name: Azure Kubernetes set context
        uses: Azure/aks-set-context@v3
        with:
          # Resource Group Name
          resource-group: 'github-action-rg'
          # AKS Cluster Name
          cluster-name: 'cck8sdemo'
          # AKS Cluster Subscription
          subscription: '98a3196f-6832-4fbc-b17d-84d0c75b73ab'
          # Get cluster admin credentials. Values: true or false
          admin: 'false'
          # Enables kubelogin for non-admin user scenario. Values: true or false
          use-kubelogin: 'true'

      # Substitute environment variables in a file
      - name: envsubst-action
        uses: danielr1996/envsubst-action@1.1.0
        env:
          MONGODB_URI: 'mongodb://ccdemocosmo:uUGdJe1mw1JAhcjaq7vEcTmk4IDKYv7X4LAmgxXRXAEoARUhUphc2PVarAfWxgpQon5apIl90L1tACDbtL9UHg==@ccdemocosmo.mongo.cosmos.azure.com:10255/?ssl=true&retrywrites=false&maxIdleTimeMS=120000&appName=@ccdemocosmo@'
          CONTAINER_NAME: hello-world-app:${{ github.sha }}
          AZURE_CONTAINER_REGISTRY: ccconreg.azurecr.io
        with:
          # File to run substitutions on
          input:
            kubernetes/hello-world-deployment.yaml
          # File to write result to
          output:
            hello-world-deployment.yaml
            
      # Deploys application based on given manifest file
      - name: Deploys application
        uses: Azure/k8s-deploy@v4
        with:
          action: deploy
          manifests: |
            hello-world-deployment.yaml
            kubernetes/hello-world-service.yaml
          images: |
            ccconreg.azurecr.io/hello-world-app:${{ github.sha }}
